# ──────────────────────────────────────────────────────────────────────────
# Full ML / API / MLOps stack — needed to run the pipeline, API, and tests.
# Installed together with requirements.txt via `make install`.
# NOT installed by Streamlit Cloud (keeps the dashboard build light).
# ──────────────────────────────────────────────────────────────────────────

# Core Data Science
scipy>=1.11.0
scikit-learn>=1.3.0

# ML Frameworks
xgboost>=2.0.0
lightgbm>=4.1.0
catboost>=1.2.0

# Explainability
shap>=0.44.0

# API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0

# MLOps
mlflow>=2.8.0
joblib>=1.3.0

# Utilities
pyyaml>=6.0.1
tqdm>=4.66.0

# Database (production data source — --source db)
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0  # PostgreSQL driver; swap for pymysql etc. for other DBs

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Notebook
jupyter>=1.0.0
ipykernel>=6.26.0
matplotlib>=3.8.0
seaborn>=0.13.0

# Code Quality
black>=23.10.0
flake8>=6.1.0
isort>=5.12.0

# Documentation
tabulate>=0.9.0
